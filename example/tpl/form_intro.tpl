<p align="center">
	This form will request new users from generator and update DB with new users
</p>
<p>
	This text is defined in template file and loaded by the function <code>ltpl::file('form_intro')</code> in <code>example_site::test_genusers()</code>.
	In form text and raw-parts could be inserted in many ways - raw HTML input, text "in-cell" input (like this text does)
</p>